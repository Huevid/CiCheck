name: Build and Deploy Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Generate timestamp
      id: timestamp
      run: echo "::set-output name=timestamp::$(date '+%Y%m%d%H%M%S')"

    - name: Build Docker image
      run: |
        timestamp=$(echo "${{ steps.timestamp.outputs.timestamp }}")
        docker build -t huveid/nginxcicd:${timestamp} -f Dockerfile .
      env:
        DOCKER_BUILDKIT: 1

    - name: Log in to Docker Hub
      run: docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_PASSWORD }}

    - name: Push Docker image to Docker Hub
      run: |
        timestamp=$(echo "${{ steps.timestamp.outputs.timestamp }}")
        docker push huveid/nginxcicd:${timestamp}

  deploy:
    runs-on: self-hosted

    needs: build

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Get timestamp from previous job
      id: timestamp
      run: echo "::set-output name=timestamp::${{ needs.build.outputs.timestamp }}"

    - name: Pull Docker image from Docker Hub
      run: |
        timestamp=$(echo "${{ steps.timestamp.outputs.timestamp }}")
        docker pull huveid/nginxcicd:latest

    - name: Run Docker container
      run: |
        docker run -d -p 80:80 --name 123 huveid/nginxcicd:latest --restart=always

    - name: Docker ps
      run: docker ps -a
      
    - name: Docker stop
      run: docker stop 123

    - name: Docker start
      run: docker start 123
      
    - name: Docker Container Logs
      run: docker logs 123

    - name: NGINX Error Log
      run: docker exec 123 cat /var/log/nginx/error.log

